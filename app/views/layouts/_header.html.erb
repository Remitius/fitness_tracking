<header class="page-header">
  <% unless params[:controller] == 'users' && params[:action] == 'new' %>
    <div>
      <%= link_to "Create Account", new_user_path %>
    </div>
  <% end %>

  <% unless params[:controller] == 'workouts' && params[:action] == 'index' %>
    <div>
      <%= link_to "Home", :root  %>
    </div>
  <% end %>
  
  <% unless params[:controller] == 'workouts' && params[:action] == 'new' %>
    <div>
      <%= link_to 'Create Workout', new_workout_path %>
    </div>
  <% end %>

  <% unless params[:controller] == 'exercises' && params[:action] == 'index' %>
    <div>
      <%= link_to "Exercise Info", :exercises_index  %>&nbsp;&nbsp;
    </div>
  <% end %>

  <%= yield %>
</header>
